# URL del backend. En local apunta al Express; en producción a la API desplegada.
VITE_API_URL=http://localhost:3000

# URL del microservicio de IA (FastAPI). En local el servicio Python;
# en producción, su URL desplegada.
VITE_AI_URL=http://localhost:8000
