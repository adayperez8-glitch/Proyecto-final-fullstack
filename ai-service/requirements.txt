# Microservicio de IA de Brote — FastAPI + LangGraph + ChromaDB + Gemini
# Versiones fijadas a las probadas en Python 3.14 (ene 2026).

# API
fastapi==0.136.3
uvicorn[standard]==0.48.0
pydantic==2.13.4
pydantic-settings==2.14.1

# Auth (verifica el mismo JWT que el backend Node, HS256)
PyJWT==2.13.0

# Base de datos (lectura de las tablas Prisma + persistencia del chat)
SQLAlchemy==2.0.50
psycopg2-binary==2.9.12

# IA: agente, RAG y modelo
langchain==1.3.2
langchain-core==1.4.0
langchain-google-genai==4.2.4
langchain-chroma==1.1.0
langgraph==1.2.2
chromadb==1.5.9
